@model (string title, string content)

<div class="dc-text-block dcg-tpt">
    @if (Model.title != "")
    {
        <h3 class="dcg-h3">@Model.title</h3>
    }
    <p>
        @Model.content
    </p>
</div>